## frontend-plan

**用途**: 做前端前先出方案，不直接开改。

**步骤**:
1. 检查当前 Vue 路由 / 视图 / 组件 / stores
2. 确认涉及的 API 端点 (查 `endpoints.ts`)
3. 确认数据模型 (查 `types.ts`)
4. 确认视觉 tokens 可用 (`tokens.css`)
5. 找出最小实现路径
6. 列出要修改的文件（精确路径）
7. 列出风险点

**输出**:
- 实现方案（3-5 句）
- 文件清单（精确路径 + 改动类型）
- 风险点
- **等待确认后再编辑代码**
